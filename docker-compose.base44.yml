services:
  web:
    image: python:3.12-slim
    working_dir: /app
    command: >
      sh -c "pip install --no-cache-dir -r requirements.txt &&
             streamlit run App.py"
    volumes:
      - ./:/app
    ports:
      - "3000:8501"
    environment:
      - PYTHONUNBUFFERED=1
